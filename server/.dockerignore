# 构建上下文忽略清单（同 .gitignore 思路）—— 打包镜像时不传这些。
#
# 关键:node_modules 不进上下文,镜像内部用 npm ci 按 lockfile 重装干净的一份;
# 传本地 node_modules 既白占带宽,又可能把宿主机(Windows)编译的原生模块带进
# Linux 镜像导致运行报错。

# 依赖(镜像内重装)
node_modules/

# 本地运行时数据(SQLite 库 + 上传图片,绝不进镜像)
data/

# 本地环境变量(密钥等,绝不进镜像)
.env
.env.*

# 测试脚本 / 开发产物(镜像只需运行时代码)
smoke-test.mjs

# 版本控制 / 编辑器 / 系统杂项
.git/
.gitignore
.DS_Store
Thumbs.db

# 日志
*.log
npm-debug.log*

# Docker 自身文件(无需进上下文)
Dockerfile
.dockerignore
